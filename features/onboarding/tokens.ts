/** Jupitrr design tokens — matches main app accent (#3C3FEF). */
export const JUPITRR = {
  primary: '#3C3FEF',
  primaryDark: '#292D8D',
  primaryLight: '#EEF0FF',
  ink: '#0B0B0F',
  muted: '#6B7280',
  surface: '#F8F9FF',
} as const;

/** Award laurel gold. Matches the star glyphs the wreath frames (Tailwind yellow-500), so
 *  the rating cluster reads as one mark rather than a grey wreath around gold stars. */
export const LAUREL_GOLD = '#EAB308';

/** Hero / demo full-bleed gradient */
export const HERO_GRADIENT: [string, string, string] = ['#3C3FEF', '#292D8D', '#0B0B0F'];

/** S3 jump-cut spotlight gradient */
export const JUMPCUT_GRADIENT: [string, string, string] = ['#3C3FEF', '#5B5EF5', '#292D8D'];
