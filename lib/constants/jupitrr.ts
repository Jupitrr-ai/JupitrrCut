/** Inbox that in-app bug reports are sent to. */
export const SUPPORT_EMAIL = 'hello@jupitrr.com';

/** Must match `androidPackage` in app.config.js. */
export const ANDROID_PACKAGE_NAME = 'com.jupitrr.aiteleprompter';
